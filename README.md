# RollNote Assets

Public library image assets for RollNote.

These files are only the shared camera, lens, and film library images. User-uploaded photos and user-created gear data stay on each user's device and are not stored here.

CDN base URL:

```text
https://cdn.jsdelivr.net/gh/PhatHadFeesh/RollNote-assets@main
```
